#include<stdio.h>
int main(){
    int a [3],i,sum = 0;

    for(i = 0 ; i < 3 ; i++){
        scanf("%d", &a[i]);
        
         }

    for(i = 0 ; i < 3 ; i++){
        sum = sum + a[i];
        
         }

         printf("the value of sum : %d\n",sum);








    return 0;
}